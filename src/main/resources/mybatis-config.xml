<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
<!-- 数据源配置交给spring去做   -->

    <!-- typeAliases：别名处理器：可以为我们的java类型起别名，别名不区分大小写 -->
    <!-- package:为某个包下的所有类批量起别名 name：指定包名，为当前包以及下面所有的后代包的每一个类都起一个默认别名（类名的小写格式）-->
    <typeAliases>
        <package name="com.pbb.pojo"/>
    </typeAliases>

    <!-- mappers：将sql映射文件（com.pbb.dap.Mapper.xml）注册到全局配置文件（mybatis-config.xml）中 -->
    <!-- resource：引用类路径下的sql映射文件 com/pbb/dao/EmployeeMapper.xml-->
    <mappers>
        <mapper resource="com/pbb/dao/BookMapper.xml"/>
    </mappers>

</configuration>